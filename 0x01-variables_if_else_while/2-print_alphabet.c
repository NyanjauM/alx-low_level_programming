#include <stdio.h>
/**
 * main prints alphabets in lower case
 * Return:Always 0 (Success)
 *
 */
int main(void)
{	char alphab;
	alphab = 'a'
	while
		(alphab <= 'z') {
		 	putchar(alphab);
			alphab ++}
	putchar('\n');
	return (0);
}
