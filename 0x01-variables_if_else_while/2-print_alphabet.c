#include <stdlib.h>
#include <stdio.h>

/**
 * main prints alphabets in lower case
 * Return:Always 0 (Success)
 *
 */
int main(void)
{
	char Alph_lower;

	Alph_lower = 'a';
	while
       		(Alph_lower <= z){
	
			putchar (Alph_lower);
			Alph_lower++:
	}
	putchar('\n')
	return (0);
}
