#include <stdio.h>
/**
 * main prints alphabets in lower case
 * Return:Always 0 (Success)
 *
 */
int main(void)
{
	for (char alphab = 'a'; alphab = 'z'; alphab++)
	{
		putchar(alphab);

	}

	putchar('\n');

	return (0);
}
